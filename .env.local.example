# Supabase 프로젝트 설정 (Project Settings > API 에서 확인)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...service_role_key...

# 관리자 목록. 이름별로 서로 다른 비밀번호를 쓰고, 각자 본인이 만든 예약만 볼 수 있음.
# 비밀번호는 서로 겹치지 않게 설정해야 함 (겹치면 로그인 시 누구 계정인지 구분 불가).
ADMIN_USERS=[{"name":"이름1","password":"비밀번호1"},{"name":"이름2","password":"비밀번호2"}]

# 로그인 세션 쿠키에 서명할 때 쓰는 비밀값 (긴 랜덤 문자열로 직접 생성해서 넣기)
# 터미널에서 생성 예시: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=change-me-too-a-long-random-string

# Vercel Cron이 자동 삭제 API를 호출할 때 쓰는 비밀값 (역시 랜덤 문자열)
CRON_SECRET=change-me-cron-secret

# 상담일 이후 예약 정보를 보관할 기간(일). 지나면 자동 삭제됨
RETENTION_DAYS=90
